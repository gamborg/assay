# Copy to .env and fill in the targets you actually want to run.
# Assay never requires all of these -- only the ones referenced by your config.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Any OpenAI-compatible endpoint: Ollama, vLLM, LM Studio, llama.cpp server.
LOCAL_BASE_URL=http://localhost:11434/v1
LOCAL_API_KEY=not-needed
